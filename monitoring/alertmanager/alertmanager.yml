global:
  resolve_timeout: 5m

route:
  receiver: platform-ops
  group_by: ['alertname', 'tenant']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h

receivers:
  - name: platform-ops
    # Configure your receiver here (Slack, PagerDuty, email, etc.)
    # Example Slack webhook:
    # slack_configs:
    #   - api_url: 'https://hooks.slack.com/...'
    #     channel: '#isolane-alerts'