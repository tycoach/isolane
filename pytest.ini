[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    isolation: Cross-tenant isolation tests (merge-blocking)
    unit: Unit tests
    integration: Integration tests
addopts = -v --tb=short